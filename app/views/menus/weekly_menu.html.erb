<div class="container">
  <div class="main-container">

<%= link_to "Refresh", weekly_menu_path(:q => @params_q, :qq => @params_qq), :method => :get %>
<%= console %>
  <div id="menu-page-title">
      <h1>Suggested weekly meal plan -- v1</h1>

      <!-- button to save the week menu only for signed-in users -->
      <% if user_signed_in? %>
         <!-- .first(7) added, because of getting more than seven meals in menus_controller.rb -->
        <%= link_to "Save this meal plan",week_menus_path(menus: @menus_v1.map{|menu| menu.id}), method: :post, class: "btn btn-lg btn-red mt-3" %>
      <% end %>
    </div>

    <div class="recipe-cards">
      <!-- .first(7) added, because of getting more than seven meals in menus_controller.rb -->
      <% @menus_v1.each do |recipe| %>
        <% @counter += 1  %>
          <div class="our-card">
            <img class="our-card-image" src="<%= recipe.img_url %>" alt="Card image cap">
            <div id="number">
              <h1><%= @counter %> </h1>
            </div>
            <div class="our-card-body">
              <h3 class="card-title"><strong><%= recipe.title %></strong></h3>
              <p class="card-text"><strong>Author</strong><br><%= recipe.author %></p>
              <p class="card-text"><strong>Ingedrients</strong><br>
                <%= recipe.ingredients_list.join(" - ") %>
              </p>
              <%= link_to "Go to recipe", recipe_path(recipe), class: "btn btn-transparent"%>
            </div>
          </div>
      <% end %>
    </div>


    <div id="menu-page-title">
      <h1>Suggested weekly meal plan -- v2</h1>

      <!-- button to save the week menu only for signed-in users -->
      <% if user_signed_in? %>
         <!-- .first(7) added, because of getting more than seven meals in menus_controller.rb -->
        <%= link_to "Save this meal plan",week_menus_path(menus: @menus_v2.map{|menu| menu.id}), method: :post, class: "btn btn-lg btn-red mt-3" %>
      <% end %>
    </div>

    <div class="recipe-cards">
      <!-- .first(7) added, because of getting more than seven meals in menus_controller.rb -->
      <% @menus_v2.each do |recipe| %>
        <% @counter += 1  %>
          <div class="our-card">
            <img class="our-card-image" src="<%= recipe.img_url %>" alt="Card image cap">
            <div id="number">
              <h1><%= @counter %> </h1>
            </div>
            <div class="our-card-body">
              <h3 class="card-title"><strong><%= recipe.title %></strong></h3>
              <p class="card-text"><strong>Author</strong><br><%= recipe.author %></p>
              <p class="card-text"><strong>Ingedrients</strong><br>
                <%= recipe.ingredients_list.join(" - ") %>
              </p>
              <%= link_to "Go to recipe", recipe_path(recipe), class: "btn btn-transparent"%>
            </div>
          </div>
      <% end %>
    </div>



    <div id="menu-page-title">
      <h1>Suggested weekly meal plan -- v3</h1>

      <!-- button to save the week menu only for signed-in users -->
      <% if user_signed_in? %>
         <!-- .first(7) added, because of getting more than seven meals in menus_controller.rb -->
        <%= link_to "Save this meal plan",week_menus_path(menus: @menus_v3.map{|menu| menu.id}), method: :post, class: "btn btn-lg btn-red mt-3" %>
      <% end %>
    </div>

    <div class="recipe-cards">
      <!-- .first(7) added, because of getting more than seven meals in menus_controller.rb -->
      <% @menus_v3.each do |recipe| %>
        <% @counter += 1  %>
          <div class="our-card">
            <img class="our-card-image" src="<%= recipe.img_url %>" alt="Card image cap">
            <div id="number">
              <h1><%= @counter %> </h1>
            </div>
            <div class="our-card-body">
              <h3 class="card-title"><strong><%= recipe.title %></strong></h3>
              <p class="card-text"><strong>Author</strong><br><%= recipe.author %></p>
              <p class="card-text"><strong>Ingedrients</strong><br>
                <%= recipe.ingredients_list.join(" - ") %>
              </p>
              <%= link_to "Go to recipe", recipe_path(recipe), class: "btn btn-transparent"%>
            </div>
          </div>
      <% end %>
    </div>



    <div id="menu-page-title">
      <h1>Suggested weekly meal plan -- FIRST SEVEN == HOW IT WORKED BEFORE SHUFFLE</h1>

      <!-- button to save the week menu only for signed-in users -->
      <% if user_signed_in? %>
         <!-- .first(7) added, because of getting more than seven meals in menus_controller.rb -->
        <%= link_to "Save this meal plan",week_menus_path(menus: @menus.first(7).map{|menu| menu.id}), method: :post, class: "btn btn-lg btn-red mt-3" %>
      <% end %>
    </div>

    <div class="recipe-cards">
      <!-- .first(7) added, because of getting more than seven meals in menus_controller.rb -->
      <% @menus.first(7).each do |recipe| %>
        <% @counter += 1  %>
          <div class="our-card">
            <img class="our-card-image" src="<%= recipe.img_url %>" alt="Card image cap">
            <div id="number">
              <h1><%= @counter %> </h1>
            </div>
            <div class="our-card-body">
              <h3 class="card-title"><strong><%= recipe.title %></strong></h3>
              <p class="card-text"><strong>Author</strong><br><%= recipe.author %></p>
              <p class="card-text"><strong>Ingedrients</strong><br>
                <%= recipe.ingredients_list.join(" - ") %>
              </p>
              <%= link_to "Go to recipe", recipe_path(recipe), class: "btn btn-transparent"%>
            </div>
          </div>
      <% end %>
    </div>



  </div>
</div>
